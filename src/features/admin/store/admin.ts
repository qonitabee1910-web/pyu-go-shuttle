// Backwards-compat shim. All data now comes from Supabase via
// `@/features/admin/services/admin.functions`. Types & constants live in
// `@/features/admin/types`. Do NOT add data or zustand state here.
export * from "@/features/admin/types";
